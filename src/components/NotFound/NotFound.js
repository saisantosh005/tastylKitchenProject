import {NotFoundMainContainer} from './styledComponents'

const NotFound = props => {
  const {onClickButton} = props
  return <NotFoundMainContainer>NotFound</NotFoundMainContainer>
}

export default NotFound
